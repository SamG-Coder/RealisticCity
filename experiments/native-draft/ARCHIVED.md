# Archived native draft

This draft was started before the user clarified that the application must use cuda-webshader in the browser. It is retained only as history and is not the runnable project. The authoritative, corrected source is now `D:/RealisticCity/kernels/building.cu`.

The old migration script and native build files in this directory are superseded. Use the root `npm run build`, `npm start`, and `npm test` commands.
